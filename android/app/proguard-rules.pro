# M-001 skeleton: no app-specific rules needed yet. R8's default Android/
# Compose/Hilt rules (bundled with each library's consumer-rules.pro) cover
# this stage. Add rules here as later modules introduce reflection-sensitive
# libraries (e.g. Room, a JSON parser).
