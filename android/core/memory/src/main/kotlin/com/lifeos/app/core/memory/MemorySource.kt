package com.lifeos.app.core.memory

/** Memory §10's confidence sources — see [ConfidenceModel] for the mapped values. */
enum class MemorySource {
    USER_STATED,
    CAREGIVER_IMPORTED,
    AI_INFERRED,
    DENIED,
}
